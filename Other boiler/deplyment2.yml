apiVersion: apps/v1
kind: Deployment
metadata:
  - name: my-deployment
    lables:
      app: nginx

spec:
  replicas: 3
  selector:
    matchLabels:
     app: nginx
  template:
    metadata:
      labels:
        app: nginx
  spec:
   container:
    - name: nginx-hi
      image: nginx:latest
      port:
        - conntainerPort: 80 
